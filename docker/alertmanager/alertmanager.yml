global:
  resolve_timeout: 5m

route:
  group_by: ['alertname']
  group_wait: 10s
  group_interval: 10s
  repeat_interval: 1h
  receiver: 'default-receiver'

receivers:
  - name: 'default-receiver'
    # Define webhook, slack, or email configs here
    # slack_configs:
    # - api_url: '<your-slack-webhook-url-here>'
